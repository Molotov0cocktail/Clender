# T78 双端 v1.3.1 发行

2026-09-15，基线main/049a1db。用户已验收Android及Windows修复，明确授权推送Release，并要求不再复杂验证。本轮复用T76/T77已验证签名APK/AAB和Windows EXE，不重跑测试、不重新构建。临时Gitee令牌仅进程内使用，不落文件/日志/提交。

范围：README发布信息、根AGENTS、本任务、progress；GitHub安全快进main、双站注释v1.3.1标签与正式Release三附件。Android1.3.1(3)，生产代码、数据、依赖、权限不改。Gitee main保留独立LICENSE历史，不强推。附件只有Clender-Windows-v1.3.1.exe、Clender-Android-v1.3.1.apk/.aab。

轻量矩阵：正常核对三产物大小/SHA256与既有验收，标签指向同一源码，Release三附件完整；边界已存在标签/Release先回读不覆盖异内容；异常上传中断仅补缺项，令牌/服务端异常不输出秘密；回归源码生产文件不变、无用户数据/密钥/缓存入提交。无重复全量门禁，用户本轮指令优先于历史每改必构建要求。

步骤：核对本地与远程→更新发行说明/文档→安全提交/推送/注释标签→创建双站Release并上传→回读发布状态与附件大小摘要→记录链接。回滚以新提交纠正，不强推、不清用户数据；若部分发布失败保留正确附件并继续补齐，不擅自删除公开Release。完成须双站均公开、三件附件匹配，报告发布提交与链接。

# T29 — 集成验证、构建、双实例冒烟与提交

## 目标

- 独立复核全部改动，完成发布级验证、数据保护和 Git 提交。

## 非目标

- 不访问真实 Provider，不推送远程，不创建快捷方式。

## 影响文件

- 测试、任务状态和必要的集成修复；`dist/Clender.exe` 仅构建替换且不提交。

## 接口/数据影响

- 无新增契约；验证 T23–T28 的组合行为。

## 风险

- 子任务交叉回归、PyInstaller QtNetwork、双实例测试残留进程、构建触碰 `dist/data/`、提交泄密。

## 实施步骤

- [ ] 独立审查 diff 与任务测试。
- [ ] 运行全量 unittest、导入、offscreen 和视觉检查、build check。
- [ ] 只读记录 `dist/data/` 摘要，完整构建并复核。
- [ ] 在隔离目录执行 exe 首/次实例与启动冒烟，精确清理测试进程/临时目录。
- [ ] 更新文档，配置指定全局代理，检查 staged diff/敏感模式并提交。

## 测试矩阵

- 正常：全功能组合、构建、首次/二次启动。
- 边界：无托盘、悬浮关闭/开启、最大化、空数据。
- 异常：次实例 IPC 失败诊断、构建失败保留旧 exe。
- 回归：dist/data 摘要不变、无真实数据/Key/缓存进入提交。

## 回滚方式

- Git 父提交恢复工程；构建发布使用单 exe 原子替换，用户数据不参与。

## 完成定义

- [x] 所有验证通过或明确记录未运行原因与风险。
- [x] 完整构建与双实例 exe 冒烟通过，dist/data 不变。
- [x] 本任务文件随最终 Git 提交交付；提交哈希在最终报告中给出。

## 实施结果

- 全量 `unittest discover -s tests -v`：89/89 通过；修改前基线为 39/39。
- 全部运行模块导入、`build.py --check`、`git diff --check` 通过。
- 三轮独立子 agent 审查覆盖模块实现、组合回归、敏感/生成物扫描与视觉截图；额外发现并修复相邻短 marker 命中、获取模型沿用旧连接、托盘临时悬浮窗跨日不刷新。
- Light/Dark 周/日/悬浮窗截图确认 lane、短 marker、`+N` overflow、右缘纹理、文字省略、列表和空态；Windows DWM 透明度、多屏/DPI、系统标题栏置顶/拖动仍属可见桌面人工风险。
- 完整 PyInstaller 构建成功，`dist/Clender.exe` 为 45,538,690 字节，SHA-256 `22481CEE59C6B11D9CD58DFD686332447E16473B824DCFFE6F52B6F97495F7E5`。
- 隔离 exe 首实例在 15 秒内创建独立 DB 并保持存活；第二实例在 10 秒内以退出码 0 结束；精确路径进程与隔离目录均已清理。
- `dist/data` 构建/冒烟前后均为 5 文件、261367 字节、组合 SHA-256 `1CC055FB177A01DDCD9524553139FF40FDA6447E04F8AC0EC2C7AFA025BB8864`。
- 早期一轮悬浮 Qt 测试可能只读开发态 config/DB、没有写入；发现后统一 mock，后续专项、全量、截图与 exe 冒烟均使用临时/隔离数据。

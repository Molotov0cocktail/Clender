# T28 — 工程规则与架构文档同步

## 目标

- 更新 AGENTS.md，显式加入规划询问、多 agent 和 Git 代理规则，并同步实际结构/契约/风险。

## 非目标

- 不改写 T01–T22 历史证据，不把真实 Key 或数据写入文档。

## 影响文件

- `AGENTS.md`、本轮 proposal/design/prompt、任务文件与 `doc/tasks/progress.md`。

## 接口/数据影响

- 文档契约改为 JSON Key、单实例、lane、相交查询和悬浮窗。

## 风险

- 新旧安全规则冲突；历史记录被误改；代理规则把 commit 与网络行为混淆。

## 实施步骤

- [ ] 在强制门禁加入规划澄清和大改多 agent 规则。
- [ ] 记录 `git config --global https.proxy http://127.0.0.1:7890` 为提交前要求。
- [ ] 更新结构、数据流、接口、测试矩阵、风险和维护记录。

## 测试矩阵

- 正常：新模块/接口/命令均可检索。
- 边界：历史 T22 保留且明确已被新决策替代。
- 异常：无真实 Key、绝对私有数据或生成物内容。
- 回归：文档命令与当前代码一致。

## 回滚方式

- 恢复本任务文档变更；不影响运行数据。

## 完成定义

- [x] AGENTS.md 已更新结构、契约、命令、风险和维护记录，并加入规划澄清、多 agent、Git 代理规则。
- [x] progress 与各任务结果同步。

## 实施结果

- 根指南显式要求：规划阶段集中确认所有不确定事项；大规模修改先写提案/设计/任务/控制提示并由独立子 agent 先测试后实现；提交前执行指定全局 HTTPS 代理命令。
- 同步 JSON Key、单实例、lane/marker/overflow、相交查询、悬浮窗、设置、详情、89 项测试、导入命令、风险和当前 Phase 8 文档入口。
- T01–T22 历史证据保留原样；旧 Credential Manager 契约只保留在明确标为历史的任务/维护记录。
